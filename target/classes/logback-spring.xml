<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE xml>
<configuration>
	 <include resource="org/springframework/boot/logging/logback/defaults.xml" />
	 <property resource="application.properties" />
	 <logger name="org.hibernate.SQL" level="debug"/> 	<!-- Este filtro se aplica a todos los perfiles -->


	<!-- Perfil para desarrollo, salida por terminal -->
	<springProfile name="dev">		
		<include resource="org/springframework/boot/logging/logback/console-appender.xml" />
		<logger name="mx.gda" level="debug" additivity="false">
        	<appender-ref ref="CONSOLE"/>
    	</logger>
    	<logger name="mx.gda" level="debug" additivity="false">
        	<appender-ref ref="CONSOLE"/>
    	</logger>
    	<root level="info">
			<appender-ref ref="CONSOLE" />
		</root>
	</springProfile>

	
	<!-- Perfil para producción, genera archivo -->
	<springProfile name="prod">
		<appender name="SAVE-TO-FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">										
			<!--<file>${logging.file.path}/log.log</file> -->
			<encoder>
      			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    		</encoder>
			<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!--  <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy"> ${logging.file.path}/${spring.application.name}_%d{dd-MM-yyyy}_%i.log -->
				<fileNamePattern>
					${logging.file.path}/${spring.application.name}_%d{dd-MM-yyyy}.log
				</fileNamePattern>
				<!-- <maxFileSize>10MB</maxFileSize>-->
				<maxHistory>8</maxHistory>
				<totalSizeCap>3GB</totalSizeCap>
			</rollingPolicy>
		</appender>
		<logger name="org.springframework.boot.SpringApplication" level="INFO" additivity="false">
    		<appender-ref ref="SAVE-TO-FILE"/>
		</logger>
		<root level="INFO">
 			<appender-ref ref="SAVE-TO-FILE"/>
 		</root> 				
	</springProfile>
		
</configuration>